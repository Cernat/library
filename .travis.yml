# The language we use
language: java

# Java Development Kit (JDK)
jdk:
  - openjdk8

dist: trusty

services:
  - docker

before_install:
  - docker-compose up

script:
  - mvn clean install

branches:
  only:
    - Travis-CI